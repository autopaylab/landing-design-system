---
"@autopaylab/landing-design-system": minor
---

Add `LeadFormSection` (organisms) — a composable lead-generation form driven by a `fields` array (text/textarea/select), for real forms that don't fit `ContactSection`'s fixed 4 fields. Also raises `.size-limit.json` budgets (molecules, organisms, root) ahead of further gap-closing components.

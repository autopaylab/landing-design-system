---
"@autopaylab/landing-design-system": minor
---

Add `Textarea` and `Select` atoms, plus `TextareaField` and `SelectField` molecules (Label + atom, matching `FormField`). Closes a real gap found auditing this package against live autopay.pl landing pages: several real contact forms need a message field or dropdown fields, and neither existed.
